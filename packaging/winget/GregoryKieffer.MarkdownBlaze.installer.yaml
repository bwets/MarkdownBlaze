# yaml-language-server: $schema=https://aka.ms/winget-manifest.installer.1.6.0.schema.json
# Inno Setup installer — per-user install that registers the .md/.markdown association.
# No code signing required (unlike the MSIX).
PackageIdentifier: GregoryKieffer.MarkdownBlaze
PackageVersion: __VERSION__
InstallerType: inno
Scope: user
Installers:
  - Architecture: x64
    InstallerUrl: https://github.com/bwets/MarkdownBlaze/releases/download/v__VERSION__/MarkdownBlaze-__VERSION__-win-x64-setup.exe
    InstallerSha256: __SHA256__
ManifestType: installer
ManifestVersion: 1.6.0
